<div class="bg-base-200 hover:bg-base-300 transition-colors p-6 rounded-2xl shadow-md">
    <h2 class="text-2xl font-bold mb-2"><%= blog.title %></h2>
    <p class="text-gray-600 mb-4"><%= blog.description %></p>
    <p class="text-sm text-gray-500">
        Posted on
        <span class="font-medium"><%= blog.date.toLocaleString() %></span>
    </p>
    <div class="flex gap-4 mt-5">
      <a href="/blog/<%= blog.slug %>" class="btn btn-primary rounded-2xl">Read</a>
      <form action="/blog/<%= blog.id%>?_request_method=DELETE" method="post" onsubmit="return confirm('Are you sure you want to delete this blog?');">
        <button type="submit" class="btn btn-error rounded-2xl">Delete</button>
      </form>
    </div>
  </div>
